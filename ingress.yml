apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: oip-webapp-dev-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - oip-webapp-dev.oip.dev.code-alpha.org
    secretName: oip-webapp-dev-tls
  rules:
  - host: oip-webapp-dev.oip.dev.code-alpha.org
    http:
      paths:
      - backend:
          serviceName: oip-webapp-dev
          servicePort: 80